using UnityEngine;

public delegate void GoombaDieEvent();

public class GoombaDieManager : MonoBehaviour
{
    public static GoombaDieEvent goombaDieEvent;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
